# LaTeX Resume

This repository contains my resume written in LaTeX.  
You can build the PDF locally using **MiKTeX** and `pdflatex`.

---

## 📦 Prerequisites

### Windows (Recommended)

Install **MiKTeX**:
- https://miktex.org/download

During installation:
- ✔ Select **"Install missing packages on the fly"**
- ✔ Choose **"Yes"** when prompted to allow automatic package installation
- ✔ Use default installation settings

After installation, restart your terminal.

---

## 🛠️ Build the Resume Locally

From the root of the repository, run:

```bash
pdflatex resume.tex
